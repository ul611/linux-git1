name: Hello

on:
  push:
    branches:
      - main
    paths:
      - 'Linux_features_42tests/README.md'
            
        

jobs:
  Hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Hello, GitHub Actions!"
